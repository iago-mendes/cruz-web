import Head from 'next/head'

import Container from '../../../styles/pages/catalogo/[company]/[line]'

const LineProducts: React.FC = () =>
{
	return (
		<Container>
			<Head>
				<title>Alklin — Produtos (Limpeza doméstica) | Cruz Representações</title>
			</Head>
		</Container>
	)
}

export default LineProducts